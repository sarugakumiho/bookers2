	  <%= render 'new', user: @user, book: @book_new%>
	  
	  
	  <div class='col-md-8 offset-md-1'>
	    
	    <h2>Book detail</h2>
	    
	    <table class='table' >
	      <tr>
	        <th>
	          <%= link_to user_path(@user) do %>
	        	<%= image_tag @book.user.get_profile_image(100,100) %>
	          <% end %>
	          <br>
	          <%= link_to user_path(@user.id) do%>
              <%= @book.user.name %>
            <% end %>
	    	  </th>
	    	 <th>
	          <%= link_to book_path(@book) do %>
              <%= @book.title %>
            <% end %>
          </th>
	    	  <th>
	    	  	<%= link_to book_path(@book) do %>
	    	  	  <%= @book.body %>
	    	    <% end %>
	    	  </th>
	        <% if @book.user == current_user %>
	   	      <th><%= link_to "Edit", edit_book_path(@book), class: "btn btn-sm btn-success edit_book_141486" %></th>
            <th><%= link_to "Destroy",@book, method: :delete, class: "btn btn-sm btn-danger destroy_book_141503", data: {confirm: "本当に消しますか？"} %></th>
          <% end %>
        </tr>
	    </table>

	  </div>
	</div>
</div>