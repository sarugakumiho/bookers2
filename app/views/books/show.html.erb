	  <%= render 'new', books: @books %>
	  
	  
	  <div class='col-md-8 offset-md-1'>
	    
	    <h2>Book detail</h2>
	    
	    <table class='table' >
	      <tr>
	        <th>
	        	<%= image_tag @book.user.get_profile_image(100,100) %>
	          <br>
	          <%= link_to user_path(@user.id) do%>
              <%= @book.user.name %>
            <% end %>
	    	  </th>
	    	 <th>
	          <%= link_to book_path(@book) do %>
              <%= @book.title %>
            <% end %>
          </th>
	    	  <th>
	    	  	<%= link_to book_path(@book) do %>
	    	  	  <%= @book.body %>
	    	    <% end %>
	    	  </th>
	        <% if @book.user == current_user %>
	   	      <th><%= link_to "Edit", edit_book_path(@book), class: "btn btn-sm btn-success edit_book_141486" %></th>
            <th><%= link_to "Dertroy",@book, method: :delete, class: "btn btn-sm btn-danger destroy_book_141503" %></th>
          <% end %>
        </tr>
	    </table>

	  </div>
	</div>
</div>